# 8G de heap + 4G de metaspace pediam mais RAM do que a máquina tem, e o daemon
# morria no meio do assembleRelease ("daemon disappeared unexpectedly").
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
# Machine has stale Android Studio installs; pin Gradle to the valid JBR.
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
